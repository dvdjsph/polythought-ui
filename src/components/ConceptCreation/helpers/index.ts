import { handleInstances } from "./handleInstances";
import { handleProperties } from "./handleProperties";
import { handleGroups } from "./handleGroups";
export { handleInstances, handleProperties, handleGroups };
